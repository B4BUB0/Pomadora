import React from 'react';

export const Button = ({ children, ...props }: any) => {
  return (
    <button {...props}>
      {children}
    </button>
  );
};

export default Button;
import React from 'react';

export const Button = ({ children, ...props }: any) => {
  return (
    <button {...props}>
      {children}
    </button>
  );
};

export default Button;
